spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/book_social_network
    username: admin
    password: secret
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto:
    show-sql: true
    properties:
      hi.hibernate.format_sql: true
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQL82Dialect
  mail:
    host: localhost
    port: 1025
    username: olexiy
    password: secret
    properties:
      mail:
        smtp:
          trust: "*"
        auth: true
        starttls:
          enable: true
          connectiontimeout: 5000
          timeout: 3000
          writetimeout: 5000
